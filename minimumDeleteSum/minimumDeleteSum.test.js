import minimumDeleteSum from './minimumDeleteSum';

describe('minimumDeleteSum', () => {
  test('is a function', () => {
    expect(minimumDeleteSum).toBeInstanceOf(Function);
  });

  // test('it returns true', () => {
  //   expect(addTwoNumbers(5)).toBeTruthy();
  // });
});